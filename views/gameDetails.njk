{% extends "layout.njk" %}

{% block top%}{% endblock %}
{% block contenu%}
  {% if game %}
    <div class="container" id="details">
    <!-- Content here -->
    <h3>Hello worlsddd</h3>
    </div>

    <div>
      <h3>{{game.name}}</h3>
      <img src="{{game.cover.url}}"/>
    </div>
    <a href="/AddGamesInToPanier?gameSelect={{game._id}}">
    <button type="button">
    Add to panier
    </button>
    </a>

    {% if addPanierValidate %}
      <p>{{game.name}} ajouter au panier avec succès !</p>
    {% endif %}

  {% endif %}

{% endblock %}
